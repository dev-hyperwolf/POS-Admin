Results returned by sub-agents of the first inventory dispatch (which fanned out and did not
compile). Kept verbatim as input to MODULE-CONTRACT-GAPS.md; the direct per-group files in
`../` supersede them where they overlap.
