{
  "name": "@hyper-tech/contracts",
  "version": "0.4.0",
  "description": "Types, enums, ids, money, time, error and event shapes shared by every Hyper-Tech backend, frontend and the POS-Admin/wm-demo estate. Zero dependencies. UMD: require() in Node, <script> in the browser, JSON exports for Python.",
  "main": "index.js",
  "types": "types.d.ts",
  "files": ["index.js", "types.d.ts", "enums.json", "schema/", "README.md"],
  "scripts": { "export": "node ../tools/contracts-export.mjs", "test": "cd .. && node --test test/contracts.test.mjs" },
  "license": "UNLICENSED",
  "private": true
}
